# Installing inventory-web

Synthetic fixture. This is the only documentation this project has: no
README, no architecture note, no licence, and the last commit is years
old.

## Build and run

Build the war file, then start it locally:

    mvn tomcat:run

## Configuration

Point the application at its database with `INVENTORY_DB_URL`, and start
it on port 8080.
