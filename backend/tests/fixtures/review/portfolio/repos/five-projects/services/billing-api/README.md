# billing-api

Invoicing service for the synthetic fixture portfolio.

Run it with `python -m billing_api.app`.

It reads `BILLING_API_TOKEN` from the environment and needs a
PostgreSQL 13 service on the private network.
