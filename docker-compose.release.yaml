# Preloop Docker Compose - Release Configuration
# Use this file for production-like deployments with tagged images.
#
# Usage:
#   PRELOOP_VERSION=0.8.0 SECRET_KEY=$(openssl rand -hex 32) \
#     docker compose -f docker-compose.release.yaml up -d
#
# Required environment variables (set in .env or export before running):
#   PRELOOP_VERSION   - Image tag (e.g., 0.8.0, latest)
#   SECRET_KEY        - Secret key for JWT token signing (generate with: openssl rand -hex 32)
#
# Optional environment variables:
#   POSTGRES_PASSWORD - Database password (default: postgres)
#   LOG_LEVEL         - Logging level (default: INFO)

services:
  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: preloop
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5

  nats:
    image: nats:alpine
    restart: unless-stopped
    command: [ "-js", "-m", "8222" ]
    healthcheck:
      test: [ "CMD-SHELL", "wget -q --spider http://127.0.0.1:8222/healthz" ]
      interval: 10s
      timeout: 5s
      retries: 5

  migrate:
    image: ghcr.io/preloop/preloop:${PRELOOP_VERSION:-latest}
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:${POSTGRES_PASSWORD:-postgres}@postgres/preloop
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY is required}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    command: python scripts/init_db.py --force

  api:
    image: ghcr.io/preloop/preloop:${PRELOOP_VERSION:-latest}
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:${POSTGRES_PASSWORD:-postgres}@postgres/preloop
      NATS_URL: nats://nats:4222
      HOST: 0.0.0.0
      PORT: 8000
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY is required}
      JWT_ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 60
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      PRELOOP_SERVICE_ROLE: api
      # Public origin this instance is reached at. Used for approval links,
      # QR pairing and CORS. Set by scripts/install-oss.sh (PRELOOP_URL).
      PRELOOP_URL: ${PRELOOP_URL:-http://localhost:3000}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-*}
      FLOW_EXECUTION_WORKER_ENABLED: ${FLOW_EXECUTION_WORKER_ENABLED:-false}
      # SMTP — required for approval emails, invitations and password resets.
      # Configure interactively via scripts/install-oss.sh or edit .env.
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USERNAME: ${SMTP_USERNAME:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM: ${SMTP_FROM:-}
      SMTP_FROM_NAME: ${SMTP_FROM_NAME:-Preloop}
    ports:
      - "8000:8000"
    command: python -m preloop.server

  gateway:
    image: ghcr.io/preloop/preloop:${PRELOOP_VERSION:-latest}
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:${POSTGRES_PASSWORD:-postgres}@postgres/preloop
      NATS_URL: nats://nats:4222
      HOST: 0.0.0.0
      PORT: 8000
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY is required}
      JWT_ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 60
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      PRELOOP_SERVICE_ROLE: gateway
      PRELOOP_URL: ${PRELOOP_URL:-http://localhost:3000}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-*}
    ports:
      - "8001:8000"
    command: python -m preloop.server

  scheduler:
    image: ghcr.io/preloop/preloop:${PRELOOP_VERSION:-latest}
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:${POSTGRES_PASSWORD:-postgres}@postgres/preloop
      NATS_URL: nats://nats:4222
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY is required}
      JWT_ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 60
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    command: preloop-sync scheduler

  worker:
    image: ghcr.io/preloop/preloop:${PRELOOP_VERSION:-latest}
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:${POSTGRES_PASSWORD:-postgres}@postgres/preloop
      NATS_URL: nats://nats:4222
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY is required}
      JWT_ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 60
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      FLOW_EXECUTION_WORKER_ENABLED: ${FLOW_EXECUTION_WORKER_ENABLED:-false}
      PRELOOP_URL: ${PRELOOP_URL:-http://localhost:3000}
      # SMTP — required for approval emails, invitations and password resets.
      # Configure interactively via scripts/install-oss.sh or edit .env.
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USERNAME: ${SMTP_USERNAME:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM: ${SMTP_FROM:-}
      SMTP_FROM_NAME: ${SMTP_FROM_NAME:-Preloop}
    # The dedicated flow-worker below filters execute_flow /
    # resume_flow_execution off the WORKQUEUE stream; consumer filters must
    # not overlap, so this pool enumerates everything else instead of using
    # the `preloop.sync.tasks.*` wildcard.
    command: preloop-sync worker --exclude-tasks execute_flow,resume_flow_execution

  flow-worker:
    image: ghcr.io/preloop/preloop:${PRELOOP_VERSION:-latest}
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:${POSTGRES_PASSWORD:-postgres}@postgres/preloop
      NATS_URL: nats://nats:4222
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY is required}
      JWT_ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 60
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      FLOW_EXECUTION_WORKER_ENABLED: ${FLOW_EXECUTION_WORKER_ENABLED:-true}
      HOSTNAME: flow-worker
    command: ["preloop-sync", "worker", "--tasks", "execute_flow,resume_flow_execution"]

  console:
    image: ghcr.io/preloop/console:${PRELOOP_VERSION:-latest}
    restart: unless-stopped
    depends_on:
      - api
      - gateway
    ports:
      - "3000:80"
    environment:
      GATEWAY_URL: http://gateway:8000

volumes:
  postgres-data:
